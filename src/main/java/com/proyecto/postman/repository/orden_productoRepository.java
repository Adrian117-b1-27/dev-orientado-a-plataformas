package com.proyecto.postman.repository;

import com.proyecto.postman.model.orden_productoEntity;
import org.springframework.data.jpa.repository.JpaRepository;

public interface orden_productoRepository extends JpaRepository<orden_productoEntity,Long> {
}
